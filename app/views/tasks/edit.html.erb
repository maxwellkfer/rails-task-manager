<%= form_for(@task) do |f| %>
  <%= f.label :title, "Name of task : " %>
  <%= f.text_field :title %>

  <%= f.label :details, "Details of task : "%>
  <%= f.text_field :details %>

  <%= f.submit %>
<% end %>
<p><%= link_to "<= back", tasks_path %></p>
